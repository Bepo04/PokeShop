<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="170dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="25dp"
    tools:context=".producte"
    android:background="@color/secondary_day">

    <ImageView
        android:id="@+id/foto"
        android:layout_width="130dp"
        android:layout_height="130dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="10dp"
        android:padding="10dp"
        android:src="@drawable/pikachu"
        android:background="@color/OnSecondary_night"/>
    <TextView
        android:id="@+id/textPokemon"
        android:layout_width="115dp"
        android:layout_height="130dp"
        app:layout_constraintTop_toTopOf="@+id/foto"
        app:layout_constraintStart_toEndOf="@+id/foto"
        app:layout_constraintBottom_toBottomOf="@+id/foto"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:text="@string/product_description"
        android:padding="10dp"/>
    <Button
        android:layout_width="100dp"
        android:layout_height="50dp"
        app:layout_constraintStart_toEndOf="@+id/textPokemon"
        app:layout_constraintBottom_toBottomOf="@+id/textPokemon"
        android:backgroundTint="@color/primary_day"
        android:textColor="@color/secondary_day"
        android:text="@string/compra"/>


</androidx.constraintlayout.widget.ConstraintLayout>